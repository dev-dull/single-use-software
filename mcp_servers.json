{
  "servers": [
    {
      "name": "filesystem",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/repo"],
      "env": {},
      "description": "Read-only access to the app directory",
      "teams": []
    }
  ]
}
